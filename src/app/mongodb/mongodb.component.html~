<h2>Acquisitions principales</h2>
<div class="container">
  <div class="row" *ngFor="let acquisition of acquisitions">
    <div class="card card-block">
      <h4 class="card-title">{{ acquisition.name }}</h4>
      <p class="card-text">{{ acquisition.body }}</p>
      <a href="#" class="card-link">Card link</a>
      <a href="#" class="card-link">Another link</a>
    </div>
  </div>
</div>
